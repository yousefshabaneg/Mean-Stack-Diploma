import { ConvertTempPipe } from './convert-temp.pipe';

describe('ConvertTempPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTempPipe();
    expect(pipe).toBeTruthy();
  });
});
